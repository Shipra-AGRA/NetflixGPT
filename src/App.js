import ReactDOM from 'react-dom/client'

const App=()=>{
    return(
        <div>hey</div>
    )
}
export default App

const root=ReactDOM.createRoot(document.getElementById("root"))
root.render(<App/>)